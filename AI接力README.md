# AI 接力 README

更新时间：2026-08-19（Asia/Shanghai）

## 合规红线

比赛规则第 9 页要求：所有控制代码须基于 MWORKS 平台完成生成，不允许二次修正。

- 正式算法只能修改 MWORKS/Sysblock 控制模型。
- 官方树莓派 GPIO、超声、电机、舵机 CCaller 可以保留。
- 禁止在控制器中新增手写 CCaller 算法。
- 禁止修改 MWORKS 生成后的 C 文件。
- 正式下载使用仓库中的 `Sysblock 外部模式工具/ExternalMode`，不用 SSH/SCP 自制运行器。

## 当前权威模型

- 纯 MWORKS 控制器：`决赛模型库/Unmanned_Vehicle_Race_2026/Controller/ObsAvoidController_Team27_MWorks.mo`
- 旧名兼容壳：`Controller/ObsAvoidController_Team27_FFLFRR.mo`，仅继承上述纯模型，无 CCaller 算法。
- 正式顶层：`for_code_JGB520_Team27.mo`，以赛会 `for_code_JGB520.mo` 为底稿，只替换黄色控制器并连接顶部扫描舵机。
- 本地生成审计：`Team27_OneClick/scripts/sysplorer_codegen.py` 与 `postprocess_generated.py`；后者只读，绝不修改生成物。

## 信号映射

| MWORKS 控制器端口 | 赛会信号 | 位置 |
|---|---|---|
| `front_dist` | sensor1 / Echo1=5 | 固定正前 |
| `fu_dist` | sensor2 / Echo2=12 | 顶部旋转超声 |
| `right_dist` | sensor3 / Echo3=13 | 固定右侧 |
| `left_dist` | sensor4 / Echo4=14 | 固定左侧 |
| `rear_dist` | sensor5 / Echo5=6 | 固定正后 |
| `fu_sensor_cmd` | u4 / PWM_PIN2=23 | 顶部舵机 |

引脚均沿用赛会模型的 wiringPi 编号。

## 控制状态

- 巡航/一般通行/避障/倒车指令为 `120/90/70/-50`；这些是官方电机接口命令，不是 cm/s。本车前进极性为电机 ID1 取负、ID2 取正。
- 固定探头为前向 45°风险扇区。常规修正目标 `±0.20`，主动绕障 `±0.52`，最终限幅 `±0.55`，每 0.05 s 最大变化 `0.06`。
- 顶部舵机在 `-0.55～+0.55` 间匀速三角扫描，每 `0.05 s` 最大变化 `0.06`；实车确认正指令朝车辆左侧。方向评分为 `0.80×扫描距离 + 0.20×固定45°距离`，固定探头仍可硬否决危险侧。
- 正前不大于 `22 cm` 进入绕障/判断；滤波正前不大于 `10 cm` 或原始有效值小于 `6 cm` 硬停车。单侧 45°原始或滤波值小于 `8 cm` 禁止转入该侧。
- 赛道按顺时针运行，同分微偏右；锁定绕障最长 `1.4 s`。失败后不允许立刻重进同一转弯，后方大于 `12 cm` 时以 `-50` 倒车，否则保持停车。
- 正式赛道不设置斜坡，坡道识别、爬坡保持和相关超时逻辑已全部删除。

## 已验证与未验证

已验证：MWORKS 控制器与正式顶层检查、正式代码生成及纯 MWORKS 合规审计；五路超声读取、前轮和顶部舵机、左右后轮动作；扫描正角物理朝左；生成代码 18 个黑盒场景全部通过，包含扫描器主导、顺时针同分、左右绕行、急停、侧向原始近距、扫描失效、限时转弯、安全倒车与实车电机极性。

未验证：当前正式控制器重新上车、超声实测尺度与信号方向、真实制动距离、波纹管反射下的稳定性和正式赛道通过率。

下一步在四轮架空条件下用官方外部模式工具部署 `for_code_JGB520_Team27`，先验收急停和转向正负，再以最低风险方式依次测试直道、单弯、S 弯和障碍。
