# Team27 树莓派异常重启排查

## 已确认现象

2026-08-20 赛道测试后，SSH 显示查询时间为 `18:30:41`，系统本次启动时间为 `18:30:03`，同时出现：

- `RunDevice.out` 进程不存在；
- `/dev/ttyAMA0` 无进程占用；
- systemd 日志提示 `journal corrupted or uncleanly shut down`；
- MWORKS 外部模式无法继续连接或停止车辆。

这说明当时不是避障状态机停在某个状态，而是树莓派发生了非正常复位或掉电。树莓派重启后，原 MWORKS TCP 会话和 `RunDevice.out` 都已消失。电机控制器可能继续保持重启前收到的最后一条速度命令，因此“电机仍在转”不能证明树莓派没有重启。

`vcgencmd get_throttled` 在重启后显示 `0x0`，只能说明当前这次开机尚未记录欠压，不能排除上一次运行结束时发生了瞬时完全掉电。

## 首选 A/B 测试

1. 关闭整车电源。
2. 断开原车给树莓派供电的正极 5 V 线路，避免两路电源反向供电。
3. 使用稳定、足额的树莓派电源或移动电源，通过 USB 接口单独给树莓派供电。
4. 电机和舵机仍由车辆电池供电；树莓派与控制板保留公共地线。
5. 先架空测试，再按相同路线进行赛道测试。

不要让原车 5 V 和 USB 电源同时给树莓派供电。不确定接线时必须让现场工程师确认。

结果解释：

- 独立供电后不再重启：检查原车 DC-DC、电源线、接头、公共地和电机干扰。
- 独立供电后轻微碰撞仍重启：检查树莓派电源插口、板载连接器、SD 卡和树莓派本体。
- 静止晃动车体或线束就重启：优先处理接触不良。
- 只有电机启动、转向或堵转时重启：优先处理瞬时压降、稳压模块容量和电机干扰。

## 硬件整改顺序

1. 测量树莓派输入端的实际 5 V，而不是只测车辆主电池的 18/24 V。
2. 用万用表最小值记录或示波器观察电机启动、舵机转向和碰撞瞬间的树莓派供电。
3. 树莓派使用独立、足额的 24 V 转 5 V 稳压供电，不与电机或大舵机共用容量不足的降压模块。
4. 电源线避免使用容易松脱的杜邦线，改用锁紧插头或可靠压接端子，并使用扎带做应力释放。
5. 检查电池、降压模块、树莓派之间的正极和地线，尤其是碰撞时会摆动的接头。
6. 电机线与超声、PWM、树莓派电源线分开布置，采用星形接地，避免电机大电流经过树莓派地线。
7. 反复非正常断电可能损坏 SD 卡，应立即备份当前可启动系统。

启动日志中的 `mcp251x spi0.0: Probe failed, err=110` 表示 SPI/CAN 外设本次启动时未正常复位。若比赛控制不使用该模块可暂时隔离观察；若使用，应检查其 3.3 V、复位线和 SPI 接头。

## 故障后现场取证

再次发生故障时，先不要重启或断电，立即 SSH 执行：

```bash
date
uptime -s
cat /proc/sys/kernel/random/boot_id
pgrep -af RunDevice.out
ps -C RunDevice.out -o pid,stat,etime,pcpu,pmem,wchan:28,cmd
sudo fuser -v /dev/ttyAMA0
vcgencmd get_throttled
dmesg -T | tail -n 80
df -h /
```

判据：

- 启动时间很新：树莓派已重启。
- `RunDevice.out` 不存在：程序退出或随重启消失。
- 进程状态为 `D`：可能阻塞于串口或硬件 I/O。
- 进程状态为 `R` 且 CPU 很高：可能停留在 GPIO/超声忙等待。
- SSH 和进程都正常、只有 MWORKS 失联：检查外部模式 TCP 会话和网络。
- 根分区接近 100%：检查日志和磁盘空间。

## 安全要求

- 供电问题解决前，必须有人守住整车总电源。
- 不启用模型自动启动。
- 必须能立即切断电机电源。
- 不得把 MWORKS“停止”按钮作为唯一急停手段。
- 树莓派重启后不应自动恢复车辆运动；若需要串口失联停车或硬件看门狗，须先向赛会技术人员确认允许的官方方案。
